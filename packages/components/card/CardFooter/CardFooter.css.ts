import { style } from "@vanilla-extract/css";

// Base footer style
export const cardFooter = style({
  flexShrink: 0,
  display: "flex",
  flexDirection: "row",
  gap: "12px",
  boxSizing: "border-box",
});
